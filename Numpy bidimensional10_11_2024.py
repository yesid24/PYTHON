import numpy as np
a = [[11,12,13], [21,22,23], [31,32,33]]
print(a)
A = np.array(a)# Convert list to Numpy Array # Every element is the same type
print(A)
print(A.ndim) # Show the numpy array dimensions
print(A.shape) # Show the numpy array shape
print(A.size)